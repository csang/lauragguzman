<?php

class Controller_Site extends Controller_App
{
	public function get_index()
	{
		// $total_fans = $this->total_fans;

		// if (isset($_COOKIE['fan'])) {
		// 	$cookie = 1;
		// 	$this->template->body = View::forge('site/index', array(
		// 		'total_fans' => $total_fans,
		// 		'cookie' => $cookie,
		// 	));
		// }else{
		// 	$this->template->body = View::forge('site/index', array(
		// 		'total_fans' => $total_fans,
		// 	));
		// }
	}
}